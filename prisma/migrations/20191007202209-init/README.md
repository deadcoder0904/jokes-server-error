# Migration `20191007202209-init`

This migration has been generated by Akshay Kadam at 10/7/2019, 8:22:09 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."Joke" (
  "author" TEXT    REFERENCES "Author"(id) ON DELETE SET NULL,
  "content" TEXT NOT NULL DEFAULT ''  ,
  "id" TEXT NOT NULL   ,
  PRIMARY KEY ("id")
);

CREATE TABLE "lift"."Author" (
  "id" TEXT NOT NULL   ,
  "name" TEXT NOT NULL DEFAULT ''  ,
  PRIMARY KEY ("id")
);

CREATE UNIQUE INDEX "lift"."Joke.content" ON "Joke"("content")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20191007202209-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,24 @@
+generator photon {
+  provider = "photonjs"
+}
+
+generator nexus_prisma {
+  provider = "nexus-prisma"
+}
+
+datasource db {
+  provider = "sqlite"
+  url      = "file:dev.db"
+}
+
+model Joke {
+  id      String @default(cuid()) @id
+  author  Author
+  content String @unique
+}
+
+model Author {
+  id    String @default(cuid()) @id
+  name  String
+  jokes Joke[]
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20191007202209-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191007202209-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
